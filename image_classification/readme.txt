1st) load the images
2nd) clean the images
3rd) resize the images into datasets with the same image size then export in a json format


4th) run inception v3, ignore the commented out cells. We need to test the image classification model with a samll number of images to see if everything is working as it should 
