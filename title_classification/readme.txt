1) run metadata automation which extracts the data in google collab and filters out bad data
2) data_set extraction prepares the data and exports the dataset
3) vector_representations trains the fasttext model and exports the vectors for each datapoint
